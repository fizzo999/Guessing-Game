'use strict';

var favoriteLanguage = prompt('What is your favorite programming language?').toLowerCase();
// favoriteLanguage = favoriteLanguage.ToLowerCase
msg = 'Your favorite is JavaScript'
if (favoriteLanguage === 'javascript') || (favoriteLanguage === 'js') {
  alert(msg.toUpperCase());

} else if (favoriteLanguage === {

}

/**
 *  add some 'if' statements or 'switch' cases that produce different 'alert' messages depending on which programming language they choose.
 *   If they chose 'JavaScript', have the 'alert' message print everything in all caps!
**/